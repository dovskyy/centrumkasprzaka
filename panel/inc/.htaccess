# Katalog z bibliotekami PHP panelu - nigdy nie jest wolany bezposrednio z przegladarki,
# tylko przez require_once z panel/*.php.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
